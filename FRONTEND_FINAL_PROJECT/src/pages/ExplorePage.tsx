import React from "react";
import { Box, Typography } from "@mui/material";

const ExplorePage: React.FC = () => {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Explore Page (Сетка фоток)</Typography>
    </Box>
  );
};
export default ExplorePage;
